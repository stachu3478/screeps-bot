const moveBoosts = BOOSTS.move as { [key: string]: { fatigue: number } }
const moveKeys: ResourceConstant[] = Object.keys(
  BOOSTS.move,
) as ResourceConstant[]
function movePartBoostAvailability(partCount: number) {}

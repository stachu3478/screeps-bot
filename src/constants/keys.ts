const enum Keys {
  mandatory = 'm',
}

const enum ShardMemoryKeys {
  ownedRooms = 'r',
  roomLimit = 'R',
}

const enum LabBoostDataKeys {
  resourceType,
  amount,
}

const enum CreepBoostDataKeys {
  name,
  resourceType,
  amount,
  mandatory,
}
